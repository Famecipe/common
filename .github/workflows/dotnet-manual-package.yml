name: .NET Package

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Package version'
        default: ''
        required: true

jobs:
  package:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Package
      run: dotnet pack -p:PackageVersion=${{ github.event.inputs.version }}
